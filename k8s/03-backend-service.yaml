apiVersion: v1
kind: Service
metadata:
  name: chatbuddy-backend
  namespace: chat-app
  labels:
    app: chatbuddy-backend
spec:
  type: ClusterIP
  ports:
  - port: 5001
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: chatbuddy-backend
